__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# train/eval are a deterministic split of the committed data/seed_postings.jsonl -
# regenerate with `python -m src.dataset` rather than tracking as separate files
data/train.jsonl
data/eval.jsonl

# LoRA adapter weights - large, regenerate locally with src/train_lora.py
models/

venv/
.venv/
*.egg-info/
.DS_Store
