# Install torch first with the CUDA build matching your GPU, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# (see AI-Visual-Inspector/SETUP_GUIDE.md - `pip install torch` alone gives
# you a CPU-only wheel that will silently make training extremely slow)
torch>=2.4
transformers==4.56.1
peft==0.14.0
accelerate==1.3.0
datasets==3.2.0
pydantic==2.13.3
requests==2.33.1

# Deliberately no bitsandbytes / 4-bit quantization: at 0.5B-1.5B params the
# base model comfortably fits in bf16 on a 10GB RTX 3080 without it, and
# bitsandbytes' Windows support has historically been unreliable - not worth
# the risk for a model this small.
